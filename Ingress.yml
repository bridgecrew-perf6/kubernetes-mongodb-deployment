apiVersion: v1
kind: Ingress
metadata:
  name: myapp-ingress
spec:
  rules:
  - host: myapp.com
    http: 
      paths:
      - backens:
          serviceName: myapp-internal-service
          servicePort: 8080
             